# 📋 项目总览

## 🎯 项目简介

Twitter Monitor 是一个自动化监控工具，可以 24/7 实时监控 Twitter 用户的推文，并在有新推文时立即发送通知到 Telegram 群组。

**适用场景：**
- 监控重要人物（如 KOL、CEO）的推文
- 实时获取行业动态
- 及时掌握市场信息
- 自动化社交媒体监控

---

## 📁 项目结构

```
twitter-monitor/
│
├── 📝 核心代码文件
│   ├── index.js              # 程序入口，初始化和启动监控
│   ├── monitor.js            # 监控核心逻辑，轮询检查新推文
│   ├── twitterApi.js         # Twitter API 封装，处理所有 API 调用
│   ├── telegramBot.js        # Telegram 通知功能，发送消息
│   └── config.js             # 配置管理，读取环境变量
│
├── ⚙️ 配置文件
│   ├── .env                  # 环境变量（敏感信息，不提交到 Git）
│   ├── .env.example          # 环境变量示例模板
│   ├── package.json          # Node.js 项目配置和依赖
│   ├── package-lock.json     # 依赖版本锁定文件
│   ├── ecosystem.config.js   # PM2 进程管理配置
│   └── .gitignore            # Git 忽略文件列表
│
├── 🚀 部署脚本
│   ├── quick-deploy.sh       # 本地一键部署到服务器（推荐）
│   ├── deploy.sh             # 服务器端自动部署脚本
│   ├── update.sh             # 应用更新脚本
│   ├── test-connection.sh    # 服务器连接和环境测试
│   └── check-config.sh       # 配置文件检查工具
│
├── 📖 文档
│   ├── README.md             # 项目主文档
│   ├── QUICKSTART.md         # 快速开始指南
│   ├── DEPLOYMENT.md         # 详细部署文档
│   ├── USAGE.md              # 使用指南
│   └── PROJECT_OVERVIEW.md   # 本文档
│
├── 📦 运行时目录
│   ├── node_modules/         # Node.js 依赖包（自动生成）
│   ├── logs/                 # 日志文件目录
│   └── last_tweet_id.txt     # 记录最后检查的推文 ID
│
└── 🗑️ 临时文件
    ├── *.log                 # 日志文件
    └── *.backup              # 备份文件
```

---

## 🔄 工作流程

```
┌─────────────────────────────────────────────────────────────┐
│                        启动流程                              │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
                    ┌──────────────────┐
                    │  读取 .env 配置  │
                    └──────────────────┘
                              │
                              ▼
                    ┌──────────────────┐
                    │ 初始化 Twitter   │
                    │    API 客户端    │
                    └──────────────────┘
                              │
                              ▼
                    ┌──────────────────┐
                    │ 初始化 Telegram  │
                    │   Bot 客户端     │
                    └──────────────────┘
                              │
                              ▼
                    ┌──────────────────┐
                    │ 获取 Twitter 用户│
                    │     信息和 ID    │
                    └──────────────────┘
                              │
                              ▼
                    ┌──────────────────┐
                    │发送启动消息到 TG │
                    └──────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      监控循环                                │
└─────────────────────────────────────────────────────────────┘
                              │
                    ┌─────────┴─────────┐
                    │                   │
                    ▼                   │
          ┌──────────────────┐          │
          │  等待指定时间     │          │
          │ (CHECK_INTERVAL) │          │
          └──────────────────┘          │
                    │                   │
                    ▼                   │
          ┌──────────────────┐          │
          │ 调用 Twitter API │          │
          │  获取最新推文    │          │
          └──────────────────┘          │
                    │                   │
                    ▼                   │
          ┌──────────────────┐          │
          │  对比上次检查的  │          │
          │   推文 ID        │          │
          └──────────────────┘          │
                    │                   │
            ┌───────┴───────┐           │
            │               │           │
            ▼               ▼           │
      ┌──────────┐    ┌──────────┐     │
      │ 没有新推 │    │ 发现新推 │     │
      │   记录   │    │   文     │     │
      └──────────┘    └──────────┘     │
            │               │           │
            │               ▼           │
            │      ┌──────────────────┐ │
            │      │ 格式化推文信息   │ │
            │      └──────────────────┘ │
            │               │           │
            │               ▼           │
            │      ┌──────────────────┐ │
            │      │ 发送到 Telegram  │ │
            │      └──────────────────┘ │
            │               │           │
            │               ▼           │
            │      ┌──────────────────┐ │
            │      │ 保存最新推文 ID  │ │
            │      └──────────────────┘ │
            │               │           │
            └───────────────┴───────────┘
                            │
                            └──────────────┘
```

---

## 🔌 API 集成

### Twitter API v2

**使用的端点：**

1. **获取用户信息**
   ```
   GET /2/users/by/username/{username}
   ```
   - 用途：根据用户名获取用户 ID
   - 返回：用户基本信息、统计数据

2. **获取用户推文**
   ```
   GET /2/users/{id}/tweets
   ```
   - 用途：获取指定用户的最新推文
   - 参数：max_results（返回数量）
   - 返回：推文列表、互动数据

**认证方式：**
- Bearer Token Authentication
- 在 HTTP Header 中传递：`Authorization: Bearer {token}`

**代理支持：**
- 支持 HTTP/HTTPS 代理
- 可配置代理认证

### Telegram Bot API

**使用的方法：**

1. **发送消息**
   ```
   POST /bot{token}/sendMessage
   ```
   - 用途：发送文本消息
   - 支持 Markdown 格式
   - 可发送到个人或群组

**消息格式：**
- Markdown 语法
- 包含推文链接
- 显示互动数据

---

## 🛠️ 技术栈

### 核心依赖

```json
{
  "axios": "^1.6.0",           // HTTP 客户端，用于 API 调用
  "dotenv": "^16.3.1",         // 环境变量管理
  "node-telegram-bot-api": "^0.64.0"  // Telegram Bot SDK
}
```

### 开发工具

- **Node.js**: JavaScript 运行时
- **PM2**: 进程管理和监控
- **Git**: 版本控制
- **Bash**: 自动化脚本

---

## 📊 数据流

```
┌──────────────┐
│  Twitter API │
└──────┬───────┘
       │
       │ 1. 获取用户信息
       │ 2. 获取最新推文
       │
       ▼
┌──────────────┐
│ twitterApi.js│
└──────┬───────┘
       │
       │ 解析和格式化数据
       │
       ▼
┌──────────────┐
│  monitor.js  │  ←─── last_tweet_id.txt (读取/保存)
└──────┬───────┘
       │
       │ 1. 对比新旧推文
       │ 2. 过滤已通知的推文
       │
       ▼
┌──────────────┐
│telegramBot.js│
└──────┬───────┘
       │
       │ 格式化通知消息
       │
       ▼
┌──────────────┐
│ Telegram API │
└──────────────┘
       │
       │
       ▼
┌──────────────┐
│   用户/群组   │
└──────────────┘
```

---

## 🔐 安全考虑

### 1. 敏感信息保护

- ✅ 所有 Token 存储在 `.env` 文件
- ✅ `.env` 文件不提交到 Git
- ✅ `.env` 文件权限设置为 600
- ✅ 不在代码中硬编码敏感信息

### 2. API 安全

- ✅ 使用官方 SDK 和库
- ✅ HTTPS 加密传输
- ✅ 支持代理隐藏真实 IP

### 3. 服务器安全

- ✅ 建议使用非 root 用户运行
- ✅ 支持 SSH 密钥认证
- ✅ 防火墙配置建议
- ✅ 日志轮转避免磁盘占满

---

## ⚡ 性能优化

### 1. 内存管理

- PM2 自动重启（超过内存限制）
- 日志自动轮转
- 定期清理临时文件

### 2. API 调用优化

- 可配置检查间隔，避免超限
- 使用 Keep-Alive 连接
- 错误重试机制

### 3. 资源占用

**典型资源占用：**
- CPU: < 1%（待机时）
- 内存: ~50-100MB
- 网络: < 1KB/s（轮询时）
- 磁盘: < 50MB

---

## 🚀 部署架构

### 单服务器部署（推荐）

```
┌─────────────────────────────────────┐
│         CentOS 7.9 服务器           │
│                                     │
│  ┌────────────────────────────┐   │
│  │         Node.js             │   │
│  └────────┬───────────────────┘   │
│           │                        │
│  ┌────────▼───────────────────┐   │
│  │          PM2                │   │
│  │   (进程管理和监控)          │   │
│  └────────┬───────────────────┘   │
│           │                        │
│  ┌────────▼───────────────────┐   │
│  │   twitter-monitor          │   │
│  │   (应用实例)               │   │
│  └────────┬───────────────────┘   │
│           │                        │
└───────────┼────────────────────────┘
            │
    ┌───────┴────────┐
    │                │
    ▼                ▼
┌─────────┐    ┌──────────┐
│Twitter  │    │Telegram  │
│  API    │    │   API    │
└─────────┘    └──────────┘
```

### 多用户监控部署

```
┌─────────────────────────────────────┐
│         CentOS 7.9 服务器           │
│                                     │
│  ┌─────────────────────────────┐  │
│  │           PM2               │  │
│  ├─────────────────────────────┤  │
│  │  twitter-monitor (user1)    │  │
│  ├─────────────────────────────┤  │
│  │  twitter-monitor (user2)    │  │
│  ├─────────────────────────────┤  │
│  │  twitter-monitor (user3)    │  │
│  └─────────────────────────────┘  │
│                                     │
└─────────────────────────────────────┘
```

---

## 📈 监控和维护

### 日志管理

**日志类型：**
- 应用日志（PM2 自动捕获）
- 错误日志（单独文件）
- 系统日志（/var/log/）

**日志轮转：**
- 最大 10MB 每个文件
- 保留最近 7 天
- 自动压缩旧日志

### 健康检查

**自动监控：**
- PM2 自动重启崩溃进程
- 内存超限自动重启
- 最小运行时间检测

**手动检查：**
```bash
pm2 status          # 查看状态
pm2 monit           # 实时监控
pm2 logs            # 查看日志
```

---

## 🔄 升级和维护

### 常规维护

**每周：**
- 检查应用状态
- 查看日志是否有异常
- 验证 Telegram 通知正常

**每月：**
- 更新依赖包（如有安全更新）
- 清理旧日志文件
- 检查磁盘空间

### 升级流程

1. 备份配置文件
2. 停止应用
3. 更新代码
4. 安装新依赖
5. 重启应用
6. 验证功能

---

## 📚 相关文档

- [README.md](README.md) - 项目主文档
- [QUICKSTART.md](QUICKSTART.md) - 快速开始
- [DEPLOYMENT.md](DEPLOYMENT.md) - 部署指南
- [USAGE.md](USAGE.md) - 使用说明

---

## 💡 扩展建议

### 功能扩展

1. **多用户监控**
   - 修改配置支持多个用户
   - 每个用户独立实例

2. **关键词过滤**
   - 只通知包含特定关键词的推文
   - 支持正则表达式

3. **定时报告**
   - 每日/每周推文摘要
   - 统计数据报告

4. **多渠道通知**
   - 支持邮件通知
   - 支持 Slack/Discord
   - 支持微信/钉钉

5. **数据存储**
   - 保存推文到数据库
   - 历史数据查询
   - 数据分析和可视化

### 架构优化

1. **分布式部署**
   - 多服务器负载均衡
   - Redis 缓存共享状态

2. **容器化**
   - Docker 容器化部署
   - Kubernetes 编排

3. **微服务化**
   - 拆分监控和通知服务
   - API Gateway 统一入口

---

**项目状态：生产就绪 ✅**

当前版本：v1.0.0  
最后更新：2025-11-02
